0，  最远基类  CmdTarget   ，对消息映射表的遍历在CmdTarget中实现。
1，  要求实现类的继承，不少三层继承关系。
2，  参照，mfc，消息映射的宏，实现如下功能。

     
     从键盘上输入数字执行对应的方法。
                 1，冒泡排序
	 2，插入排序
                 3，选择排序
 
3，  不能使用if else ，swtch case ，等分支操作。